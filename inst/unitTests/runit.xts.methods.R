#
# RUnit tests for the following 'xts' methods:
# rbind
# cbind
# c
#

# create timeSeries object from sample_matrix
#data(sample_matrix)

# create corresponding 'ts' object
#sample.ts1 <- ts(sample_matrix, start=as.numeric(as.Date(rownames(sample_matrix)[1])))
#sample.ts1.xts <- as.xts(sample.ts1)

# create corresponding 'data.frame' object

# create corresponding 'timeSeries' object

# create corresponding 'irts' object

# create corresponding 'ts' object

# create corresponding 'its' object

# create corresponding 'irts' object

# create corresponding 'zoo' object

#test.convert_ts_to_xts <- function() {
  #checkIdentical(sample.xts.ts1,as.xts(sample.ts1))
#}


#test.ts_reclass <- function() {
  #checkIdentical(sample.ts1,reclass(as.xts(sample.ts1)))
#}

# monthly series
#sample.ts12 <- ts(sample_matrix,start=1990,frequency=12)
#sample.xts.ts12 <- as.xts(sample.ts12)

#test.convert_ts12_to_xts <- function() {
  #checkIdentical(sample.xts.ts12,as.xts(sample.ts12))
#}

#test.ts12_reclass <- function() {
#  checkIdentical(sample.ts12,reclass(as.xts(sample.ts12)))
#}
